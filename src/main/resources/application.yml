cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}
    region:
      static: ${AWS_REGION}
    stack:
      auto: false
    sqs:
      queue:
        name: ${SQS_QUEUE_NAME}
        url: ${SQS_QUEUE_URL}
